export type SmuleRecordingInfo = {
  song: string;
  performers: string[];
  originalPerformer: string;
  fileName?: string;
  description: string;
  url?: string;
}

export default SmuleRecordingInfo;
